{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Run Book Program",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/library",
            "args": [],
            "cwd": "${workspaceFolder}",
            "MIMode": "lldb",
            "preLaunchTask": "build cpp"
        }
    ]
}